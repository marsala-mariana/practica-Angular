import { MarcketNamePipe } from './marcket-name.pipe';

describe('MarcketNamePipe', () => {
  it('create an instance', () => {
    const pipe = new MarcketNamePipe();
    expect(pipe).toBeTruthy();
  });
});
